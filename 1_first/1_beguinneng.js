
    // Data Types:
    // undefined, null, boolean, string, symbol, number and object

    /*var myName = 'eliseu' nesse caso, ao declarar variavel desse jeito o seu valor pode ser alterado no futuro, como podemos ver abaixo

    myName = 8; o valor da variavel a cima foi alterado

    let ourName = 'freeCodecamp' nesse caso esse tipo de variavel é declarado dentro de um escopo que vc pretende usar

    const pi =3.14; nesse caso o valor da variavel nao pode ser alterado*/

    //Storing Values with 
    //Assignment Operator

    var a;
    var b = 2;
    console.log(a)
    a = 7;

    b = a;

    console.log(a);
